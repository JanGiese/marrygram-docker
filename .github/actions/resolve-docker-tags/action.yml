name: Resolve docker tags

inputs:
  token:
    required: true
  commit_tag:
    required: false

outputs:
  frontend:
    value: ${{ steps.tags.outputs.frontend || steps.tags-default.outputs.frontend  }}
  backend:
    value: ${{ steps.tags.outputs.backend || steps.tags-default.outputs.backend }}
  media:
    value: ${{ steps.tags.outputs.media || steps.tags-default.outputs.media }}
  comment_id:
    value: ${{ steps.tags.outputs.comment_id }}

runs:
  using: composite
  steps:
    - id: tags
      uses: actions/github-script@v7
      if: ${{ github.event.pull_request.number }}
      env:
        REPO_OWNER: ${{ github.repository_owner }}
        REPO: ${{ github.repository }}
        ISSUE_NUMBER: ${{ github.event.pull_request.number }}
        COMMIT_TAG: ${{ inputs.commit_tag }}
      with:
        github-token: ${{ inputs.token }}
        script: |
          const owner = process.env.REPO_OWNER;
          const repo = process.env.REPO.replace(owner + '/', '');
          const issue_number = Number(process.env.ISSUE_NUMBER);
          const commitTag = process.env.COMMIT_TAG;

          const marker = '<!-- integration-test -->';

          const defaultTags = {
            frontend: 'latest',
            backend: 'latest',
            media: 'latest',
          };

          if (repo === 'marrygram-backend') defaultTags.backend = commitTag ? commitTag : 'development';
          if (repo === 'marrygram-frontend') defaultTags.frontend = commitTag ? commitTag : 'development';
          if (repo === 'marrygram-media-compressor') defaultTags.media = commitTag ? commitTag : 'development';

          const { data: comments } = await github.rest.issues.listComments({
            owner,
            repo,
            issue_number,
          });

          let comment = comments.find(c => c.body?.includes(marker));

          if (!comment) {
            const body = `
            ${marker}
            ### 🧪 Integration tests

            **Docker images**
            - marrygram-frontend: \`${defaultTags.frontend}\`
            - marrygram-backend: \`${defaultTags.backend}\`
            - marrygram-media-compressor: \`${defaultTags.media}\`

            **Status**
            ⏳ Waiting for results...
            `;

            comment = (
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body,
              })
            ).data;
          }

          const extract = (name) =>
            comment.body.match(new RegExp(`${name}:\\s*\\\`([^\\\`]+)`))?.[1];

          core.setOutput('comment_id', comment.id);
          core.setOutput('frontend', extract('marrygram-frontend') ?? defaultTags.frontend);
          core.setOutput('backend', extract('marrygram-backend') ?? defaultTags.backend);
          core.setOutput('media', extract('marrygram-media-compressor') ?? defaultTags.media);
    - shell: bash
      if: ${{ !github.event.pull_request.number }}
      id: tags-default
      run: |
        echo "frontend=latest" >> $GITHUB_OUTPUT
        echo "backend=latest" >> $GITHUB_OUTPUT
        echo "media=latest" >> $GITHUB_OUTPUT

